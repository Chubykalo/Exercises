# function that checks if an integer is a palindrome

def isPalindrome(self, x: int) -> bool:
    if str(x) == str(x)[::-1]:
        return True
    return False

# def is_palindrome(integer):
#
#     palindrome = str(integer)[::-1]
#     if palindrome == str(integer):
#         print('True')
#         return True
#     else:
#         print('False')
#         return False
#
#
# is_palindrome(1551)

# can i solve it without converting an integer to a string? ---- Nope
